建立
components（放置静态组件的文件夹）
routers（放置配置路由规则的文件夹）
views（放置动态增删改查组件的文件夹）
三个文件夹

在APP.vue文件中，引入需要的结构文件，我们发现会没有样式，再利用
import 导入所需要的botoostrpCSS样式文件。
把头部组件中抽离出来，放置到刚刚设置的静态组件的文件夹中
然后在APP.vue文件中，利用import AppHeader 导入分离出去的组件，
并且注册局部组件，然后再头部的位置使用。